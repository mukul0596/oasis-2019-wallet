import * as stall from '../actions/stalls';

const initailState = {};

const stall = (state = initailState, action) => {
    const tyep = {action};

    if(type === stall.GET_STALLS){
        
    }
}
